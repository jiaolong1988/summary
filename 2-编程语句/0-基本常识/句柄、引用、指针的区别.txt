句柄（Handle）
	定义：句柄通常是一个唯一的标识符，用于指向一个特定的资源或对象。它在操作系统、图形界面、数据库等场景中广泛使用。

引用（Reference）
	定义：引用是一种变量类型，它存储的是另一个变量的地址。在引用被创建时，它会指向一个已经存在的对象或变量。
	      引用通常用于指向对象，而不是基本类型的数据。


指针（Pointer）
	定义：指针是一个变量，它存储的是另一个变量的内存地址。通过指针可以访问和操作内存中的数据。
		  指针提供了访问内存的直接方式，可以用于动态内存分配、函数参数传递等。
		  